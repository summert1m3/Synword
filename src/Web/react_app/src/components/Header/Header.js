import React from "react";
import "./header.css";

const Header = () => (
    <header>
        <div className="header__inner">
            <div className="header__logo">
                synword
            </div>
        </div>
    </header>
);

export default Header;