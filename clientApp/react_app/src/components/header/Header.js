import React from "react";

const Header = () => (
    <header>
        <div className="header__inner">
            <div className="header__logo">
                synword
            </div>
        </div>
    </header>
);

export default Header;